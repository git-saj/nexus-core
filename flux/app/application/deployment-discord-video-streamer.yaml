apiVersion: apps/v1
kind: Deployment
metadata:
  name: discord-video-streamer
  namespace: discord-video-streamer-app
spec:
  replicas: 1
  selector:
    matchLabels:
      pod: discord-video-streamer
  template:
    metadata:
      labels:
        pod: discord-video-streamer
    spec:
      containers:
        - image: ghcr.io/git-saj/discord-video-streamer:latest@sha256:fcb32f6d3a4b3811bb2079a2615ef0e2c7eaed9d2d57421341a71e82e24d40f5 # {"$imagepolicy": "flux-system:discord-video-streamer"}
          name: discord-video-streamer
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
              nvidia.com/gpu: 1
            limits:
              nvidia.com/gpu: 1
          volumeMounts:
            - mountPath: /app/config.json
              name: config-volume
              subPath: config.json
              readOnly: true
      volumes:
        - name: config-volume
          secret:
            secretName: discord-video-streamer-config
